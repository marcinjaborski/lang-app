import styled from "styled-components";

export const NotePageStyled = styled.main`
  display: flex;
  justify-content: center;

  .drawer-button {
    position: fixed;
    top: 1em;
    right: 1em;
  }
`;
